apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch-ui
spec:
  project: default
  replicas: 1
  selector:
    matchLabels:
      app: elasticsearch-ui
  template:
    metadata:
      labels:
        app: elasticsearch-ui
    spec:
      containers:
      - image: docker.elastic.co/elasticsearch/elasticsearch:6.6.1
        name: elasticsearch-ui
        env:
        - name: discovery.type
          value: single-node
        ports:
        - containerPort: 9200
          name: client
        - containerPort: 9300
          name: nodes
        volumeMounts:
        - name: data
          mountPath: /usr/share/elasticsearch/data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: elasticsearch

